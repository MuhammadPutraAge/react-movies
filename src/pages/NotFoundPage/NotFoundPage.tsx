import { NotFoundText } from "../../GlobalStyle";
import { NotFoundPageContainer } from "./NotFoundPageStyles";

const NotFoundPage = () => {
  return (
    <NotFoundPageContainer>
      <NotFoundText>Page Not Found</NotFoundText>
    </NotFoundPageContainer>
  );
};

export default NotFoundPage;
